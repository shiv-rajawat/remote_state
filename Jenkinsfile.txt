pipeline {
  agent any

  stages {
    stage('Initial setup for ELK') {
        steps {
	    	sh "aws s3 ls"
          	sh "terraform init"
		sh "terraform apply -auto-approve"
		sh "terraform destroy -auto-approve"
        }
      }
    }
  }
