pipeline {
  agent any

  stages {
    stage('Initial setup for ELK') {
        steps {
	    	sh "cat ./.aws/credentials"
          	sh "terraform init"
		sh "terraform apply -auto-approve"
        }
      }
    }
  }
